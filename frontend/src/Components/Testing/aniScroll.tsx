
export default function aniScroll(props: { id: number }) {
    const id = props.id;
    return (
        <div>
            <h1>aniScroll Component {id}</h1>
            <p>This is a placeholder for the aniScroll component.</p>
        </div>
    );
}